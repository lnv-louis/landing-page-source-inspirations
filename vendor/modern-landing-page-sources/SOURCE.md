# Modern landing page template source batch

These source-backed pages come from five MIT-licensed landing page/template repositories supplied by the user.

The hosted corpus copies static production output from each template into `public/pages/<slug>/` via `scripts/generate-corpus.mjs`. Cloudflare only builds this repository; it does not build these upstream templates.
